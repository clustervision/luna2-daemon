#
# This file is written to support the CI/CD process of Gitlab.
# Status: Production
# copyright: Copyright 2026, Luna2 Project
# license: GPL
#
# Build and publish the pip package. The build/publish logic, the branch-to-stream
# mapping and the CI/CD variables it needs all live in
# ClusterVision/gitlab-cicd-pipelines, which also repoints the target directory's
# "latest" symlinks at the build it just made and prunes older ones.
#

include:
  - project: 'ClusterVision/gitlab-cicd-pipelines'
    file: '/pip-package-pipeline.yml'
    ref: 'main'
