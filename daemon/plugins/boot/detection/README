# DETECTION / Switch port node detection PLUGIN
#
#        two defined methods are mandatory:
#        - find   returns true,switch,port on success,  false on no success
#        - scan   runs through a dict of switches. collects ports+macs

#
# switchport is the default plugin for node detection
# plugin/detection/switchport.py

# ---- other plugins:

# cloud is a provided plugin to discover a cloud booing node
# plugin/detection/cloud.py

# custom is a plugin to allow for a customized node detection
# plugin/detection/custom.py
